{% extends "forms/field.html.twig" %}

{% block field %}
<div class="form-columns {{ field.classes }}">
{% if field.fields %}
    {% set cols = field.fields|length %}
    {% for child in field.fields %}
        {% set value = child.value(child.name) %}
        {% include ["forms/fields/#{child.type}/#{child.type}.html.twig", 'forms/fields/column/column.html.twig'] with {'cols':cols}  %}
    {% endfor %}
{% endif %}
</div>
{% endblock %}
